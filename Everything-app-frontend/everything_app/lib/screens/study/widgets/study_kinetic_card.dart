import '../../../widgets/kinetic_card.dart';

export '../../../widgets/kinetic_card.dart';

/// Die Karte des Studium-Space ist inzwischen die Kartenprimitive der ganzen App und liegt
/// deshalb unter lib/widgets/. Der alte Name bleibt als Alias bestehen, damit die rund 30
/// Fundstellen im Studium-Space unveraendert bleiben.
typedef StudyKineticCard = KineticCard;
